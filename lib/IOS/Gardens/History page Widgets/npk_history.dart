import 'package:flutter/material.dart';

class NpkHistory extends StatelessWidget {
  const NpkHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
