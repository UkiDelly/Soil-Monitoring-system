import 'package:flutter/material.dart';

class WebGardenCard extends StatelessWidget {
  const WebGardenCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      child: Card(child: Text("Hello")),
    );
  }
}
